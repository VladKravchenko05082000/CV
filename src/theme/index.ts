import { fontWeight, fonts, mediaQueries, transition, breakpoints, siteWidth } from "./base";
import { colors } from "./colors";

export const theme = {
  siteWidth,
  mediaQueries,
  colors,
  fontWeight,
  fonts,
  transition,
  breakpoints,
};
